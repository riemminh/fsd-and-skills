# Agent Map

Generated by `pnpm repo:index`. Read this first, then open only the linked feature or flow file.

## How To Route A Prompt

1. If the prompt matches a flow trigger, open that flow file.
2. If the prompt names a route, use the route ownership table below.
3. If the prompt names a domain noun, open `.repo-knowledge/features/<feature>.md`.
4. If a cache note conflicts with source code, source code wins; inspect the referenced file before editing.

## Flow Triggers

- `create-order` (`create order`, `order total`, `order form`, `stock validation`, `order RBAC`) -> `.repo-knowledge/flows/create-order.md`

## Utility Map

- Money, status, stock, and fulfillment logic -> `.repo-knowledge/UTIL_MAP.md`

## Feature Lookup

- `auth` -> `.repo-knowledge/features/auth.md`; root: `src/features/auth`; imports features: none
- `customers` -> `.repo-knowledge/features/customers.md`; root: `src/features/customers`; imports features: none
- `orders` -> `.repo-knowledge/features/orders.md`; root: `src/features/orders`; imports features: `auth`, `customers`, `products`; debt notes: 12
- `products` -> `.repo-knowledge/features/products.md`; root: `src/features/products`; imports features: none

## Route Ownership

- `/` -> `src/app/page.tsx` | features: `auth`
- `/dashboard` -> `src/app/dashboard/page.tsx` | features: `auth`, `orders`
- `/login` -> `src/app/login/page.tsx` | features: `auth`
- `/orders` -> `src/app/orders/page.tsx` | features: `auth`, `orders`
- `/orders/create` -> `src/app/orders/create/page.tsx` | features: `auth`, `customers`, `orders` legacy-imports
- `/products` -> `src/app/products/page.tsx` | features: `products`
- `/products/[id]/edit` -> `src/app/products/[id]/edit/page.tsx` | features: `products`
- `/products/create` -> `src/app/products/create/page.tsx` | features: `products`

## Repo Shape

- Source files indexed: 180
- Features indexed: 4
- Legacy source files: 57
- Architecture debt notes: 12

- `app`: 9
- `config`: 3
- `core`: 6
- `data`: 4
- `features`: 62
- `legacy`: 57
- `shared`: 39

## Agent Guardrails

- Prefer feature public APIs from `src/features/<feature>/index.ts` when importing from outside a feature.
- Prefer relative imports inside the same feature.
- Treat cross-feature imports and legacy imports as existing migration debt, not as examples for new code.
- Keep route files thin: route -> feature component/hook -> API/schema/utils.
